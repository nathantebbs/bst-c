# BST-C
A binary search tree CLI tool to create, test, and represent binary search tree problems.
### Install
> NOTE: currently the CLI implementation will just run your current src/main.c file.
> Once the ASCII representation is complete more CLI functionality and tooling will
> be implemented. This is not meant to be a tool yet!
```
make && sudo make install
```
